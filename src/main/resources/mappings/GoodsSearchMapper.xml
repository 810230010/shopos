<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wuliangit.shopos.dao.GoodsSearchMapper" >

    <!--企业直销商品专区-->
    <select id="goodsTypeSearch" resultType="com.wuliangit.shopos.dto.ApiGoodsListDTO">
        select goods_id, name, store_name, price, title_img, salenum, carriage, area, type from t_goods
        <where>
            type = #{type}
            <if test="searchKey != null and searchKey != '' ">
                and ( name like CONCAT('%', #{searchKey, jdbcType=VARCHAR}, '%')
                or ad_word like CONCAT('%', #{searchKey, jdbcType=VARCHAR}, '%')
                or store_name like CONCAT('%', #{searchKey, jdbcType=VARCHAR}, '%'))
            </if>
        </where>
        order by ${order}
    </select>


</mapper>